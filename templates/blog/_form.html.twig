{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.file) }}
    {{ form_row(form.content) }}
    <button type="submit" class="btn btn-primary">{{ button_name }}</button>
{{ form_end(form) }}